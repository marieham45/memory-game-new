import React from 'react';

const InstructionAddAnimals = () => {
    return (
        <h1 style={{fontSize: "2.5rem"}}>
            Tap a box to add an animal before the time is up!
        </h1>
    );
};

export default InstructionAddAnimals;